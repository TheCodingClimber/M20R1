Drop your real car model here as:

`public/models/car.glb`

The hero scene tries models in this order:

1. `/models/car.glb`
2. Procedural fallback coupe

If you replace `car.glb`, the scene auto-fits the model to the hero without changing code.
